# v0.32.1 — Explosive Finder Fixes

1. **Deep links from Actions**: /api/jobs enrichment builds `/explosive?sector=<selection>` from the job's meta for explosive-scan rows (the page already honored `?sector=`; the static registry href discarded the selection).
2. **Sector Reports list**: on "All Sectors", every individual sector scan renders as a tappable row (name, age, tickers analyzed, top-4 pick chips, running spinner) — tapping switches the dropdown to that sector's full report. All sector caches load on mount so the list survives reloads. Empty state suppressed when the list has content.
3. Research All order: 8 sectors first, combined "all" summary last; the "all" result is labeled "combined cross-sector summary" to clarify it's one AI call across every ticker (3-5 picks), distinct from the per-sector reports.
