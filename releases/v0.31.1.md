# v0.31.1 — Actions Page Polish + Nav Reorder

- Route rename: /jobs → /actions (server redirect kept at /jobs); page title "Actions"; JobsIndicator + ProfileMenu link updated. /api/jobs unchanged (internal contract).
- Status summary cards (Running/Completed/Failed within the loaded 150) that toggle the corresponding filter.
- Trigger badges moved into a fixed-width (52px) column between the status dot and the label — aligned across rows; chips shrunk to 9px caps.
- Registry gained `href` per kind; /api/jobs enriches rows with it; JobRow becomes a keyboard-accessible clickable card (router.push) with stopPropagation on Cancel and error-expand. market-monitor and daily-briefing have no output page — not clickable.
- BottomNav: Home · Options (label rename, href /plays unchanged) · Portfolio (center) · Learn (4th) · More; Learn extracted from the base tabs array so Portfolio can sit center.
