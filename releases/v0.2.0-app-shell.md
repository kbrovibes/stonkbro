# v0.2.0 — App Shell & Navigation

## Summary
Core app layout with header, bottom nav, and empty state pages for Discover, Portfolio, and Settings.

## Files changed
| File | Change |
|---|---|
| `src/app/layout.tsx` | Modified — Added Header, BottomNav, max-width container |
| `src/app/globals.css` | Modified — Stone palette from snobaddy |
| `src/app/page.tsx` | Modified — Discovery empty state |
| `src/app/portfolio/page.tsx` | Created — Portfolio empty state |
| `src/app/settings/page.tsx` | Created — Settings with coming soon cards |
| `src/components/Header.tsx` | Created — Sticky header with branding |
| `src/components/BottomNav.tsx` | Created — Fixed bottom nav with 3 tabs |

## Notes
- No auth — all pages publicly accessible for now (auth is backlog item #12)
- Bottom nav uses Heroicons-style inline SVGs
- Active tab detection uses pathname matching
