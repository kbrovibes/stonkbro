# v0.10.1 — Force Email Test Button

## Summary
Manual trigger to send a full email briefing on demand, for e2e testing of the Resend email pipeline.

## Dependencies added
- `@google/generative-ai` (was already imported but missing from package.json)

## Files changed
| File | Change |
|---|---|
| `src/app/api/email/test/route.ts` | Create — POST route that runs the cron email pipeline for the authenticated user |
| `src/app/(app)/settings/page.tsx` | Modify — load settings on mount, persist via API, add "Send Test Email" button |
| `BACKLOG.md` | Update — #47 moved to Done |
| `CHANGELOG.md` | Update — v0.10.1 entry |

## Notes
- The test email route reuses the exact same pipeline as `/api/cron` but scoped to a single authenticated user instead of all users via admin client.
- Settings page `handleSave()` was previously a stub (only toggled local state). Now it POSTs to `/api/settings`.
- Button is disabled until an alert email is saved.
